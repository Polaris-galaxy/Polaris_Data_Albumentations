# Data_Albumentations — 全仓库 pip 依赖汇总
#
# 若有 Blender 插件脚本，其使用内置模块 `bpy`，勿在此安装。
#
# PyTorch：若需 CUDA，请从 https://pytorch.org 按本机环境选择安装命令（可只装 CPU 版）。

# 通用：图像读写、增强、YOLO 标注（文件名后缀 _中文 为作用说明；如 yolo_albumentations_augment_to_target_YOLO增强至目标张数.py 等）
numpy>=1.24
opencv-python>=4.9
albumentations>=2.0
Pillow>=10.0
PyYAML>=6.0

# PyTorch 生态：deformable_detr_gpu_infer_file_camera_GPU视频与摄像头检测.py, camera/detr_opencv_camera_detect_OpenCV摄像头检测.py 等
torch>=2.0
torchvision>=0.15

# 可视化：verify_yolo_augmentation_matplotlib_增强效果可视化验证.py
matplotlib>=3.7
