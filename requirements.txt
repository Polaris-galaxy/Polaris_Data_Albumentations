# Data_Albumentations — 全仓库 pip 依赖汇总
#
# 非 pip：`picture_yolo/class_float_fix.py` 使用 Blender 内置模块 `bpy`，需在 Blender 中运行，勿在此安装。
#
# PyTorch：若需 CUDA，请从 https://pytorch.org 按本机环境选择安装命令（可只装 CPU 版）。

# 通用：图像读写、增强、YOLO 标注（fixed_augmentation.py, yolo_augmentation.py, albumentations_use.py, video_data_get, label_checker 等）
numpy>=1.24
opencv-python>=4.9
albumentations>=2.0
Pillow>=10.0
PyYAML>=6.0

# PyTorch 生态：gpu_detect.py, misc_fix.py, camera/deformable_detr_camera.py 等
torch>=2.0
torchvision>=0.15

# 可视化：yolo_annotation_augmentation/verify_augmentation(test).py
matplotlib>=3.7
